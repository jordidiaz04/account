server:
  port: '8081'

spring:
  profiles:
    active: local
  application:
    name: account

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: 'true'
    restart:
      enabled: 'true'