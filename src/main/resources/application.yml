server:
  port: '8081'

spring:
  application:
    name: account
  config:
    import: optional:configserver:http://localhost:9000
  cloud:
    config:
      profile: dev

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: 'true'
    restart:
      enabled: 'true'